# Frontend (Vite) env. Only VITE_-prefixed vars are exposed to the browser.
# NEVER put the Anthropic key here — it lives in the BFF's .env only.

# Where the dev server proxies /api calls (the BFF). Defaults to localhost:3001.
VITE_BFF_URL=http://localhost:3001
